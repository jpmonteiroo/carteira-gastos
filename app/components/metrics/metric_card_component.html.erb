<div class="metric-card">
  <p class="text-sm text-slate-500"><%= title %></p>
  <p class="mt-3 text-3xl font-semibold text-slate-950"><%= value %></p>
  <p class="mt-2 text-sm <%= subtitle_classes %>"><%= subtitle %></p>
</div>
